lang=zig
corelib=corelib-zig
framework=zig-fuzz
pacemaker=false
chunked_decode=push
encode_surfaces=new,stream
min_output_buffer=1
# CORELIB_PLAN §5.1 pass-through permission: README — this port grants no pass-through permission
pass_through=no
