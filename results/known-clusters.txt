# Accounted-for cluster camps — the baseline `oracle/cluster.py --baseline` diffs against.
#
# A camp belongs here ONLY once it is explained: a catalogued finding, a legal
# divergence, or a benign soft axis. Anything else is reported as NEW and the run
# exits non-zero. That is deliberate — the nightly has clustered and uploaded its
# result since it was written, nobody read the artifact, and nine unexplained camps
# accumulated unnoticed until 2026-08-03. An unread artifact is not a signal.
#
# The signature is the (driver-set, verdict) partition ONLY — not the representative
# and not the accepted bytes — so it survives the corpus changing under it.
#
# IT DOES NOT SURVIVE THE ROSTER CHANGING. Every signature names every driver, so
# adding cpp-fixed and cpp-c-cpp-dyn (crucible#129) invalidated all ten entries at
# once and the 2026-08-05 nightly reported "9 NEW CAMPS" — six of which were these
# same rows with the two new names inside them. A mechanism that cries wolf on a
# roster change trains people to stop reading it, which is the exact failure it was
# built to prevent. Rebased then; making it survive a roster change is in docs/TODO.md.
#
# A FIXED CAMP IS DELETED, NOT KEPT. A row whose cause has been repaired would match
# that camp again if it ever came back, reporting a regression as "known" — which is
# how F-0054's row was handled when it closed, and why its return was caught twice.
# So this file shrinks when the family improves.
#
# Rebased 2026-08-06 against sofabgen 0.0.0-20260806101130-dec1e42049cd and every
# corelib at its main tip, over the 9502-input corpus: 3884 agree, 5618 diverge ->
# **4 camps, all explained**. Nine rows were deleted in that pass, listed at the
# bottom for the record.
#
# Format: <signature>  # <label>

I:c,cpp,cpp-c-cpp,cpp-c-cpp-dyn,cpp-fixed,csharp,dart,go,py-cython,py-pure,rust-nostd,rust-std,typescript,zig | I:java  # benign — java emits an incomplete_value payload on I; the verdict is unanimous (policy incomplete_value: soft)

# --- deleted 2026-08-06, cause repaired -------------------------------------------
# The fixlen header hook landed in all five push/visitor corelibs (corelib-cs#53,
# -java#62, -rs#47, -rs-no-std#68, -zig#37) and the backends now consume it, so the
# maxlen / element-id half of F-0043 converged: its five catalogued vectors all agree.
# These rows are gone rather than kept, per the rule above — if any comes back it must
# read as NEW:
#
#   I:csharp,py-cython,py-pure,rust-nostd,rust-std,zig | I:java | reject:...  (over_len_blob at the word)
#   I:py-cython,py-pure,zig | reject:...                                      (the py deferral row)
#   I:zig | reject:...                                                        (its payload-completion residue)
#   I:csharp,dart,go,rust-nostd,rust-std,zig | I:java | reject:...            (string_array over-index)
#   I:csharp,dart,go,rust-nostd,rust-std,typescript,zig | I:java | reject:... (string_array, maxlen 64)
#   I:csharp,rust-nostd,rust-std,zig | I:java | reject:...                    (over_len_string, further offset)
#   I:csharp,rust-nostd,rust-std,typescript,zig | I:java | reject:...         (the same with typescript late)
#
# Also deleted, a different cause:
#
#   I:dart,go,typescript,zig | reject:...   legal — the CORELIB_PLAN §6.4 mid-payload MAY.
#       documentation#40 turned that MAY into a MUST NOT on 2026-08-05, so the camp is
#       not merely unexercised, it would now be a conformance bug. It must read as NEW.
#
# The two unattributed camps from nightly 31074707585 (I:java alone; I:typescript,zig)
# are also gone — they were never baselined, and they no longer occur.

# --- deleted 2026-08-16, cause repaired -------------------------------------------
# generator#267 closed 2026-08-11 (the fixlen-header hook): the schema-bound verdict now
# lands at the length/element word, so F-0043's three camps no longer occur. Verified by
# removing the sweep_malform_truncate carve-out (43 -> 96 vectors, all green) and by four
# clustering passes over the 17870-input corpus in which none of them appeared. Deleted
# rather than kept, per the rule above — if any returns it must read as NEW:
#
#   I:dart,go,py-cython,py-pure,typescript | reject:c,cpp,cpp-c-cpp,cpp-c-cpp-dyn,cpp-fixed,csharp,java,rust-nostd,rust-std,zig  # F-0043 — declared-width bound, truncated (generator#267), the surviving half of that finding
#   I:dart,go,py-cython,py-pure | reject:c,cpp,cpp-c-cpp,cpp-c-cpp-dyn,cpp-fixed,csharp,java,rust-nostd,rust-std,typescript,zig  # F-0043 — the same declared-width bound with typescript in the correct camp; both partitions occur in the corpus
#   I:c,cpp,cpp-c-cpp,cpp-c-cpp-dyn,cpp-fixed,csharp,dart,go,py-cython,py-pure,rust-nostd,rust-std,zig | I:java | reject:typescript  # F-0043, finer offset — over-index element truncated INSIDE the fixlen word; only typescript decides at the earliest decidable point. Also the whole-message camp of F-0061's r3 reproducer
